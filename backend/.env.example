# VOTA.PE Backend Environment Variables
# Copy this file to .env and fill in the values

# Server
PORT=4000
NODE_ENV=development

# CORS — set to your frontend URL in production
CORS_ORIGIN=http://localhost:3000

# Database (optional — if not set, uses in-memory DB)
# DATABASE_URL=postgresql://user:password@host:5432/votape
