import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-geist-sans",
  subsets: ["latin"],
  weight: ["300", "400", "500", "600", "700", "800", "900"],
});

export const metadata: Metadata = {
  title: "VOTA.PE — Inteligencia Electoral en Tiempo Real",
  description: "Plataforma política digital en tiempo real. Visualiza candidatos, rankings, momentum político y coordina la intención de voto nacional.",
  keywords: "elecciones perú, candidatos, votación, ranking político, encuesta",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="es">
      <body className={`${inter.variable} antialiased`}>
        {children}
      </body>
    </html>
  );
}
